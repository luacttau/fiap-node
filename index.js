const sum = (x, y) => {
    return x + y
}

const minus = (x, y) => {
    return x - y
}

const mult = (x, y) => {
    return x * y
}

const divide = (x, y) => {
    return x / y
}

module.exports = {sum, minus, mult, divide}